{% extends 'public/base.html.twig' %}

{% block title %}Espace administrateur - Prodige Studio{% endblock %}

{% block contenu %}
    <section class="section conteneur" style="padding-top: 7rem;">
        <div class="panel">
            <p class="label-or">Back office</p>
            <h1 class="section-titre">Espace <span class="accent">administrateur</span></h1>
            <p class="section-note">Connecte en tant que {{ user.email }}.</p>

            <div class="panel-liens">
                <a href="{{ path('admin_prestation_index') }}" class="panel-lien">Gerer les prestations</a>
                <a href="{{ path('app_logout') }}" class="panel-lien panel-lien--sortie">Se deconnecter</a>
            </div>
        </div>
    </section>
{% endblock %}
