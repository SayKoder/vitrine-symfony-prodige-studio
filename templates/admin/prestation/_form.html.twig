{{ form_start(form) }}
    {{ form_row(form.nom) }}
    {{ form_row(form.description) }}
    {{ form_row(form.prix) }}
    {{ form_row(form.dureeMinutes) }}
    {{ form_row(form.actif) }}
    <button type="submit">Enregistrer</button>
{{ form_end(form) }}
